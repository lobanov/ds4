 of radius $a$ with its centre at a distance $d$ from the centre of the conductor ($d > a$). A point charge $q$ is placed inside the cavity at a distance $b$ from the centre of the conductor ($b < d$). Find the force on the charge $q$